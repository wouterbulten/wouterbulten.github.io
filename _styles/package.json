{
  "name": "styles",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "minify": "node-sass main.scss --output-style compressed --output ../assets/styles/min && postcss --no-map.inline --use autoprefixer postcss-flexbugs-fixes -o ../assets/styles/min/main.css ../assets/styles/min/main.css && node inline-css.js",
    "watch": "node-sass --watch . --output ../assets/styles"
  },
  "repository": {
    "type": "git",
    "url": "github.com/123test/api-sdk"
  },
  "author": "Wouter Bulten",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "autoprefixer": "^6.5.3",
    "bootstrap": "^4.0.0-alpha.6",
    "node-sass": "^3.11.2",
    "postcss-cli": "^2.6.0",
    "postcss-flexbugs-fixes": "^2.1.0"
  },
  "dependencies": {
    "critical": "^0.8.4"
  }
}
